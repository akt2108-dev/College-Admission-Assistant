# Copy this file to backend/.env and fill with real values.
# Never commit real secrets.

DB_NAME=admission_bot
DB_USER=postgres
DB_PASSWORD=2108@Aman
DB_HOST=localhost
DB_PORT=5432

# Optional tuning
DB_MAX_CONNECTIONS=10

# Comma-separated list. Use explicit origins in production.
# Example:
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=http://127.0.0.1:5500,http://localhost:3000